{
  "name": "backend",
  "version": "1.0.0",
  "description": "AI-powered autonomous testing platform backend",
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "npm run playwright:ensure && tsc -p tsconfig.json",
    "playwright:ensure": "node scripts/ensure-playwright-browsers.js",
    "playwright:install": "playwright install chromium",
    "postinstall": "npm run playwright:ensure",
    "start": "npm run playwright:ensure && node dist/server.js"
  },
  "keywords": [
    "qa",
    "playwright",
    "testing",
    "crawler"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@types/nodemailer": "^8.0.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.6.1",
    "nodemailer": "^9.0.0",
    "playwright": "^1.55.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.3"
  }
}
